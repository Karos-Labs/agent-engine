{
  "name": "@agent-engine/tools",
  "private": true,
  "version": "0.0.1",
  "description": "The Layer 3 tool registry (RFC-01 §9): a unified export of all seven karos-* MCP tool servers, on the file+git local adapter.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "npm run typecheck && vitest run"
  },
  "dependencies": {
    "@agent-engine/core": "^0.0.1",
    "@agent-engine/tool-common": "^0.0.1",
    "@agent-engine/tool-karos-client": "^0.0.1",
    "@agent-engine/tool-karos-gates": "^0.0.1",
    "@agent-engine/tool-karos-intel": "^0.0.1",
    "@agent-engine/tool-karos-ledger": "^0.0.1",
    "@agent-engine/tool-karos-memory": "^0.0.1",
    "@agent-engine/tool-karos-publish": "^0.0.1",
    "@agent-engine/tool-karos-research": "^0.0.1",
    "@agent-engine/tool-karos-seo-geo": "^0.0.1",
    "@agent-engine/tool-karos-topics": "^0.0.1",
    "zod": "^4.4.3"
  }
}
