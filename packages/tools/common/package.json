{
  "name": "@agent-engine/tool-common",
  "private": true,
  "version": "0.0.1",
  "description": "Shared Layer 3 tool infrastructure: the AgentTool factory, typed-outcome helpers, and the file+git local adapter every karos-* tool server builds on.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "npm run typecheck && vitest run"
  },
  "dependencies": {
    "@agent-engine/core": "^0.0.1",
    "@agent-engine/telemetry": "^0.0.1",
    "zod": "^4.4.3"
  }
}
