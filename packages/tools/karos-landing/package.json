{
  "name": "@agent-engine/tool-karos-landing",
  "private": true,
  "version": "0.0.1",
  "description": "The Landing Builder v2 tool layer (RFC-11): capture the client's current site, deterministic page checks, headless render + screenshots, GCS archive, and Firebase Hosting deploy (preview channel -> live).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "pretest": "node ../../../scripts/check-dist-freshness.mjs",
    "test": "npm run typecheck && vitest run"
  },
  "dependencies": {
    "@agent-engine/core": "^0.0.1",
    "@agent-engine/tool-common": "^0.0.1",
    "google-auth-library": "^10.9.1",
    "playwright": "1.53.0",
    "zod": "^4.4.3"
  }
}
