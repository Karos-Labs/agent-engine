PREFERRED APPROACH: fold the connected/unconnected mapping into the existing scoring config as a single additive google_overlay{} block (REUSE-FLAG-1 fix — one source of scoring truth, no separate score-time loader file), plus three minimal additive blocks in the capture and metrics configs. NO weight, bucket-total, normalization, target, or band in any validated config is changed. The scorer reads config LEFT-JOIN overlay on rec_id/metric_key; an input with no overlay entry behaves exactly as today.

EDIT 1 — config/seo-geo-scoring-config.json (4 additive blocks; NO weight/bucket/band changes)

1a. ADD a top-level "google_overlay" block (folds in what was previously a standalone overlay file). Contents = version "a3-google-overlay-v1"; the four connector definitions (key, google_product, oauth_scope_readonly, default_consent, api, read_methods_allowlist, provides, snapshot_hash, cron, per_client_gate for crux, scope_hardening for gbp); source_ladder enum [first_party_google, measured_ai_capture, research_estimate] + terminal UNAVAILABLE; data_source_enum + confidence_enum (confidence_enum INCLUDES "estimated_proxy" for GEO-28 and "measured_field" for SEO-04); inputs[] keyed by rec_id/metric_key (SEO-04, GEO-01, GEO-41, GEO-28, GEO-30, GEO-29, BOTH-NEW-01, GEO-14, and the classic-ranking family keyed on metric_key ONLY — no catalog rec_id, since BOTH-RANK-01 does not exist in the catalog [WORKS-UNCONNECTED gap 3]); each input carries connected_source.reads, connected_confidence, unconnected_fallback.reads, unconnected_confidence, degradation note; and google_own_recommendations {ingest_as research_runs.payload.recommendations[] source:'google_connected', topic->rec_id dedup map, omit-when-unconnected rule}.

1b. ADD to "constants": "unconnected_sentinel": "UNCONNECTED".

1c. APPEND to "reproducibility.hash_inputs" (L756-768, currently 12 entries) the four names at positions 13-16 after "entity_snapshot_hash": "gsc_snapshot_hash","ga_snapshot_hash","crux_snapshot_hash","gbp_snapshot_hash". GATED on Daniel's sign-off (appending to the hashed input list is a determinism-contract change, back-compatible only via the sentinel-collapse). ADD a sibling "reproducibility.google_layer_rule": these resolve to literal "UNCONNECTED" on a Layer-2 run; the digest writer DROPS any overlay hash == sentinel before the 0x1F-join with NO residual separator and the four hashes hold STABLE indices 13-16 (partial connect keeps survivors at their index — no downstream byte shift, Defect-3); connecting un-drops the hash => one drift_event + version_bump; GEO-01/41 verified flips source from gsc_snapshot_hash ONLY with crawl_snapshot_hash.gsc_verified FROZEN at capture value and drift_event.changed_fields listing gsc_snapshot_hash not crawl_snapshot_hash (Defect-1); crux_snapshot_hash un-drops on a per-client opt-in NOT the global PSI key (Defect-2). UPDATE the inputs_digest note (L770) to say the join spans up-to-16 fields with the sentinel-collapse rule (keep the existing 0x1F construction and the "all fields INSIDE the digest, no sibling carve-out" guarantee). APPEND gsc_/crux_/gbp_snapshot_hash to reproducibility.dependency_split.seo_and_readiness (L772+) and gsc_snapshot_hash to the visibility split.

1d. ADD one rule to "normalization_fns": "data_source_overlay_rule" — per input the effective measured value is read from connected_source.reads when its snapshot hash != "UNCONNECTED" (and, for crux, the per-client opt-in is set), else from unconnected_fallback.reads; the chosen data_source + confidence are FROZEN onto input_values[rec_id].{data_source,confidence} and are inside the run hash; overlay selection NEVER alters normalization/weight/target/bands (SEO-04 lab->field is the sole legitimate measured-value change).

WHY: One inline overlay block (no parallel config store), one digest-set change (4 appended names + collapse + Defect-1/2/3 rules), one constant, one source-selection rule. Zero rubric math touched. GEO-01/41's gsc_verified/gsc_ai_optout stay FROZEN inside crawl_snapshot_hash exactly as today (L68/L329/L770) and the connect-time flip is attributed to gsc_snapshot_hash.

EDIT 2 — config/seo-geo-capture-config.json (3 small additive notes; engines[] UNCHANGED)

2a. ADD "engine_vs_gsc_note": Google Search Console is a first-party SITE-DATA source (gsc_snapshot_hash via the overlay), NOT an AI-answer engine; Gemini remains the Google AI-answer engine; a GSC connection never adds an engine column.

2b. ADD to response_set.per_prompt_engine_fields a "capture_source_enum_alignment" mapping the existing MEASURED/MEASURED_grounded/ESTIMATED/UNAVAILABLE 1:1 onto the overlay confidence enum (measured/measured_grounded/estimated/unavailable), and note GEO-28's unconnected state maps to "estimated_proxy" (proxy substitution, not the same metric degraded — WORKS-UNCONNECTED gap 2).

2c. APPEND to "open_scoring_decisions" a "google_connect_layer" note: Layer-1 sources flow through the overlay and DO NOT change N/N_e (visibility denominators are engine cells; GSC is not an engine); the only visibility-touching upgrade is GEO-28's proxy->measured swap, which moves data_source not the formula.

WHY: Preserves the load-bearing engine-vs-GSC distinction and aligns the per-cell tier vocabulary with the overlay; no engine list or capture math changes.

EDIT 3 — config/seo-geo-metrics-config.json (registry: data_source dimension + new DETAIL metrics + provenance note; NO new tables)

3a. In provenance: rename the "twelve_hashes" note to "hash_set", APPEND the four overlay hashes, ADD "google_overlay_hashes" + a "back_compat_collapse" note (sentinel-drop before the 0x1F-join => fully-unconnected overlay run == prior 12-field v2 digest; overlay hashes at stable indices 13-16).

3b. ADD the NEW per-metric dimension to every seo_geo metric row: "data_source" (current/default reality), "confidence_default", "upgrades_when_connected": bool, "overlay_rec_id": <rec_id|null>. Defaults reflect the unconnected reality (e.g. seo.score = public_crawl/measured; seo.bucket.technical_cwv = upgrades_when_connected:true, overlay_rec_id:"SEO-04").

3c. ADD new client-safe DETAIL metric rows for the connected signals, keyed on metric_key (no dangling catalog rec_id): seo.gsc.avg_position (lower_better, hidden_until_connected), seo.gsc.impressions, seo.gsc.clicks (provenance_hashes ["gsc_snapshot_hash"], overlay_rec_id null), geo.ga.ai_referrals (overlay_rec_id "GEO-29", provenance_hashes ["ga_snapshot_hash"], unconnected_state connect_prompt).

3d. ADD to canonical_store/provenance.point_links: research_runs.payload.hashes now spans up-to-16 frozen hashes (overlay hashes carry "UNCONNECTED" when Layer 2); client_benchmarks.evidence.data_source + .confidence echoed (client-safe) FROM the overlay selection; raw connected query rows -> seo_geo_run_internals only. ADD to surfacing.hidden_from_client: OAuth/refresh tokens, scope strings, and connection-existence-for-other-clients are never on any anon table (token table is zero-anon); the portal Connect/Connected chip is DERIVED from authenticated portal context, not a wire-readable clients column (FLAG-2 fix).

WHY: Delivers the registry's data_source/confidence dimension the brief asks for and surfaces the connected DETAIL KPIs, with NO new canonical tables (the 4 hashes ride research_runs.payload.hashes; raw values ride seo_geo_run_internals; client-safe KPIs project to dashboard_snapshots like any DETAIL metric).

NO EDITS NEEDED TO: the validated weight/bucket structures in seo-geo-scoring-config.json (35+25+25+15 and the 28+22+15+12+5+8+10 bucket totals are untouched); engines[] in seo-geo-capture-config.json; any existing table schema. ONE NEW zero-anon table client_google_tokens is infra, not a config edit. NO anon-readable connection-flag column is added (the former clients.google_connected_at flag is REMOVED from the design — FLAG-2 fix; portal state derives from authenticated context).

DROPPED FROM THE PRIOR DRAFT:
- EDIT 0 (the standalone seo-geo-google-overlay-config.json file) is REMOVED — the overlay is folded inline into EDIT 1a so there is no second score-time config loader (REUSE-FLAG-1). If a doc-only copy is ever kept for reference it must never be read at score time.
- clients.google_connected_at anon flag is REMOVED (FLAG-2) — replaced by authenticated-context derivation of the Connect/Connected chip.
