{
  "name": "@agent-engine/tool-karos-publish",
  "private": true,
  "version": "0.0.1",
  "description": "Capability-scoped, draft-first publishing.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "npm run typecheck && vitest run"
  },
  "dependencies": {
    "@agent-engine/core": "^0.0.1",
    "@agent-engine/tool-common": "^0.0.1",
    "zod": "^4.4.3"
  }
}
