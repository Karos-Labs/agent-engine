{
  "name": "@agent-engine/core",
  "private": true,
  "version": "0.0.1",
  "description": "BaseAgent, AgentContext, ModelRouter, and the shared type contracts every agent and workflow step is built on.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "npm run typecheck && vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "openai": "^7.4.0",
    "zod": "^4.4.3"
  }
}
