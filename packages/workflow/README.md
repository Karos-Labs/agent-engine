# @agent-engine/workflow

Layer 1 durable-workflow primitives: step.code, step.agent, step.gate, fanout, and the gate() signal. Firestore adapter first, Postgres and Temporal adapters later. See section 8.

**Not implemented yet.** See the relevant section of [`docs/RFC-01-agent-engine-core.md`](../../docs/RFC-01-agent-engine-core.md) for the spec this package implements.
