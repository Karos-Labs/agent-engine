{
  "name": "@agent-engine/dynamic-sandbox",
  "private": true,
  "version": "0.0.1",
  "description": "Out-of-process execution for admin-authored dynamic-agent code steps.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "npm run typecheck && vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
