{
  "name": "@agent-engine/agent-branded-shorts",
  "private": true,
  "version": "0.0.1",
  "description": "The Branded Shorts agent (RFC-06 migration) \u2014 a deterministic video-rendering pipeline (brand resolve, transcribe, edit/cut, highlights, color, graphics & cutaways, build, self-eval QA gate) with two bounded islands of model judgment, plus the one-time per-client Style Exploration onboarding workflow.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "pretest": "node ../../scripts/check-dist-freshness.mjs",
    "test": "npm run typecheck && vitest run"
  },
  "dependencies": {
    "@agent-engine/core": "^0.0.1",
    "@agent-engine/tool-common": "^0.0.1",
    "@agent-engine/tool-karos-scraper": "^0.0.1",
    "@agent-engine/tool-karos-video": "^0.0.1",
    "@agent-engine/tools": "^0.0.1",
    "@agent-engine/workflow": "^0.0.1",
    "zod": "^4.4.3"
  }
}
