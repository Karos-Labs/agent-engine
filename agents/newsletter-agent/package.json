{
  "name": "@agent-engine/agent-newsletter",
  "private": true,
  "version": "0.0.1",
  "description": "The Newsletter / email agent — the RFC-02 §5 migration: one BaseAgent (NewsletterDraftAgent) plus its Layer 1 workflow, built entirely on Phase 1's core/tools/workflow packages, following the same recipe as the X, LinkedIn, Reddit, and Blog agents.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "npm run typecheck && vitest run"
  },
  "dependencies": {
    "@agent-engine/core": "^0.0.1",
    "@agent-engine/tool-common": "^0.0.1",
    "@agent-engine/tool-karos-gates": "^0.0.1",
    "@agent-engine/tools": "^0.0.1",
    "@agent-engine/workflow": "^0.0.1",
    "zod": "^4.4.3"
  }
}
