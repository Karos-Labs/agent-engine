{
  "name": "@agent-engine/agent-x",
  "private": true,
  "version": "0.0.1",
  "description": "The X (Twitter) agent \u2014 the RFC-02 pilot migration: one BaseAgent (XDraftAgent) plus its 16-step Layer 1 workflow, built entirely on Phase 1's core/tools/workflow packages.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "pretest": "node ../../scripts/check-dist-freshness.mjs",
    "test": "npm run typecheck && vitest run"
  },
  "dependencies": {
    "@agent-engine/core": "^0.0.1",
    "@agent-engine/tool-common": "^0.0.1",
    "@agent-engine/tool-karos-gates": "^0.0.1",
    "@agent-engine/tool-karos-scraper": "^0.0.1",
    "@agent-engine/tools": "^0.0.1",
    "@agent-engine/workflow": "^0.0.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@agent-engine/evals": "^0.0.1"
  }
}
